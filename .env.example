# .env.example

# ── Database ───────────────────────────────────────────────────────────
# SQLAlchemy / Cloud SQL URL format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL=postgresql://user:password@localhost:5432/petscream

# ── Stripe ─────────────────────────────────────────────────────────────
# Your Stripe secret key
STRIPE_API_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXX
# Your Stripe webhook signing secret
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXXXX

# ── SendGrid ────────────────────────────────────────────────────────────
SENDGRID_API_KEY=SG.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Printful ────────────────────────────────────────────────────────────
PRINTFUL_API_KEY=pf_XXXXXXXXXXXXXXXXXXXXXXXX

# ── Google Cloud Storage ────────────────────────────────────────────────
GCS_BUCKET_NAME=your-gcs-bucket-name

# ── Celery / Redis ───────────────────────────────────────────────────────
# e.g. redis://:PASSWORD@REDIS_HOST:6379/0
CELERY_BROKER_URL=redis://:password@redis-server:6379/0

# ── Flask Frontend ───────────────────────────────────────────────────────
# URL of your FastAPI backend API
API_URL=http://localhost:8000/api

# ── Google Credentials ───────────────────────────────────────────────────
# Path (inside container) to your GCP service‐account JSON
GOOGLE_APPLICATION_CREDENTIALS=/path/to/sa-key.json